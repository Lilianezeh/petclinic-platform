apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: petclinic-dev-vets-service
  namespace: argocd
spec:
  project: default
  source:
    repoURL: https://github.com/Lilianezeh/petclinic-platform.git
    targetRevision: main
    path: helm/petclinic-service
    helm:
      valueFiles:
        - ../../helm-values/vets-service.yaml
  destination:
    server: https://kubernetes.default.svc
    namespace: petclinic-dev
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
